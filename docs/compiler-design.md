# Compiler design

The detailed compiler and runtime design is maintained as a standalone Simplified Chinese
document: [compiler design (简体中文)](compiler-design.zh-CN.md).

The public architecture is shared canonical IR followed by the reference interpreter or the
adaptive CoreCLR JIT. The Chinese document covers source fidelity, verification, runtime ABI,
logical GC, deoptimization, and deployment boundaries.
