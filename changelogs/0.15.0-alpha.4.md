# Lunil 0.15.0-alpha.4

- Keep dynamic symbol resolution unavailable when runtime dynamic code is disabled; exact registered bindings remain the only supported route for AOT or trimmed hosts.
- Keep the new public FFI enums compatible across the .NET 10 and `netstandard2.1` package assets.
- Preserve the existing package/native searcher contract: `package.loadlib` remains an explicit unsupported Lua C module diagnostic.
