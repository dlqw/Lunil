# Lunil 0.15.0-alpha.4

- Qualify the FFI registry-only path in the NativeAOT/trimmed fixture and keep dynamic symbol resolution unavailable when runtime dynamic code is disabled.
- Add release-oriented validation inputs for the opt-in FFI surface, including platform loader boundaries, native lifecycle, package integration, and public API/build checks.
- Add the required package-validation suppressions for the framework-defined `ISpanFormattable` difference on the new public FFI enums.
- Preserve the existing package/native searcher contract: `package.loadlib` remains an explicit unsupported Lua C module diagnostic.
- Freeze the implementation, tests, candidate changelogs, and release evidence for final review.
