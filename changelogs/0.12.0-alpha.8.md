# Lunil 0.12.0-alpha.8

## Fixed

- Patch acceptance now reports an explicit `Expired` result when the signed manifest has reached
  its expiry time.
- Direct and coordinated commits recheck manifest expiry at the update safe point, before any
  candidate is constructed or executed. An expired prepared patch returns
  `LuaPatchCommitStatus.Expired` without mutating live state.
