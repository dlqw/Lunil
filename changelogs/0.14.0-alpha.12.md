# Lunil 0.14.0-alpha.12

- Added an optional compact syntax arena for parse-only and incremental workloads while immediate binding uses the materialized tree directly, avoiding a redundant full-tree copy.
- Made detailed code-reference collection opt-in at the binder boundary and enabled it automatically for workspace indexing, so standalone compiler pipelines do not pay workspace-only indexing costs.
